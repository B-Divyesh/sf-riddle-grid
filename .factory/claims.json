[
  {
    "id": "unique-solutions",
    "claim": "All 20 daily layouts have exactly one solution.",
    "where": "README",
    "test": "npm test -- --grep @claim:unique-solutions",
    "sandbox": "deterministic solver; enumerate all row and column permutations for every authored puzzle; assert one valid layout each"
  },
  {
    "id": "daily-puzzle",
    "claim": "One shared puzzle is selected for each UTC date.",
    "where": "landing page and README",
    "test": "npm test -- --grep @claim:daily-puzzle",
    "sandbox": "deterministic core; select the puzzle twice for the same fixed date; assert equal puzzle data and a valid id"
  },
  {
    "id": "sample-complete",
    "claim": "The sample lets you solve the grid or view its explanation.",
    "where": "demo introduction and README",
    "test": "npm test -- --grep @claim:sample-complete",
    "sandbox": "fresh browser context at ?demo=1; solve the shipped sample and assert its result and score; restart, make three incorrect checks, and assert the explanation"
  },
  {
    "id": "demo-isolation",
    "claim": "The sample opens at ?demo=1 and saves progress and sound separately from daily play.",
    "where": "landing action, demo banner, README, and demo guide",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; seed daily progress and sound, open /?demo=1, assert separate demo settings, then reset, leave through How it works, and Start for real while asserting only demo keys are removed"
  },
  {
    "id": "restart-reset",
    "claim": "Restart sample clears the sample layout.",
    "where": "demo result action",
    "test": "npm test -- --grep @claim:restart-reset",
    "sandbox": "fresh browser context at /demo; solve the sample; restart; assert all grid cells are empty"
  },
  {
    "id": "hint-cost",
    "claim": "A hint reveals one exact position and costs one score leaf.",
    "where": "game hint action and README",
    "test": "npm test -- --grep @claim:hint-cost",
    "sandbox": "fresh browser context at /demo; reveal all four hints; assert an exact position appears and score labels use 4, 3, 2, 1 leaf, then 0 leaves"
  },
  {
    "id": "failed-checks",
    "claim": "Three incorrect checks open the solution explanation.",
    "where": "README and game status",
    "test": "npm test -- --grep @claim:failed-checks",
    "sandbox": "fresh browser context at /demo; submit the same incorrect complete layout three times; assert the explanation opens"
  },
  {
    "id": "sound-setting",
    "claim": "The sound setting stays in this browser.",
    "where": "landing privacy section and README",
    "test": "npm test -- --grep @claim:sound-setting",
    "sandbox": "fresh browser context at /demo; turn sound off; reload; assert the muted state remains"
  },
  {
    "id": "local-progress",
    "claim": "Progress stays in this browser.",
    "where": "landing facts, privacy page, and README",
    "test": "npm test -- --grep @claim:local-progress",
    "sandbox": "fresh browser context at /demo; place one specimen; reload; assert the same cell remains filled"
  },
  {
    "id": "keyboard-controls",
    "claim": "The puzzle works with keyboard controls.",
    "where": "README",
    "test": "npm test -- --grep @claim:keyboard-controls",
    "sandbox": "fresh browser context at /demo; select with Enter; move across grid with arrows; place with Enter"
  },
  {
    "id": "phone-60fps",
    "claim": "The game runs at 60 frames per second on the mid-range phone test profile.",
    "where": "README and handoff",
    "test": "npm test -- --grep @claim:phone-60fps",
    "sandbox": "fresh Chromium context at 390x844 with device scale factor 2 and 4x CPU throttling; measure three one-second requestAnimationFrame samples; assert the median is 55-65 fps"
  },
  {
    "id": "free-to-play",
    "claim": "The complete game is playable without payment.",
    "where": "landing facts",
    "test": "npm test -- --grep @claim:free-to-play",
    "sandbox": "fresh browser context at /?demo=1; solve the shipped sample and assert the end screen appears with no payment, purchase, or subscription controls"
  },
  {
    "id": "private-static-game",
    "claim": "Playing a complete sample sends no data; the site uses no analytics, ads, cookies, or third-party scripts.",
    "where": "privacy page and demo guide",
    "test": "npm test -- --grep @claim:private-static-game",
    "sandbox": "fresh browser context at /?demo=1; record the initial load and a full run with a hint; assert interactions add no requests, every request is an allowlisted static GET with no body or extra query, cookies are empty, and no analytics, ads, or external scripts exist"
  },
  {
    "id": "no-third-party",
    "claim": "The game loads no files from other websites.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:no-third-party",
    "sandbox": "fresh browser context at /demo; record requests through a complete run with a hint; assert every request is an allowlisted same-origin static GET with no request body or query payload"
  },
  {
    "id": "puzzle-choices-local",
    "claim": "Puzzle choices stay in this browser.",
    "where": "landing privacy section, privacy page, and README",
    "test": "npm test -- --grep @claim:puzzle-choices-local",
    "sandbox": "fresh browser context at /?demo=1; record the complete sample run; assert interactions create no requests, all initial requests match the static allowlist without payloads, and choices are stored under the demo localStorage key"
  },
  {
    "id": "no-account-required",
    "claim": "The complete game works without an account.",
    "where": "privacy page heading",
    "test": "npm test -- --grep @claim:no-account-required",
    "sandbox": "fresh browser context at /?demo=1; assert no account, sign-in, registration, form, or profile controls exist; complete the sample and reach the result"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for service worker; set context offline; reload; assert the sample heading is visible"
  }
]
